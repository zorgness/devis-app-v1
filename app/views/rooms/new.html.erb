<h1>new room</h1>

<%= simple_form_for [@quotation, @room] do |f| %>
  <%= f.input :name, collection: @collection,:include_blank => true %>
  <%= f.button :submit, class: 'btn btn-gradient'%>
<% end %>
